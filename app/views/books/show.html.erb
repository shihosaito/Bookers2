<div class="container">
	<div class="row">
		<%= render 'users/sidebar', user:@user, book:@book %>

		<div class="col-md-8" >

			<h1>Book detail</h1>
			<table class="table">
				<thead>
					<tr>
						<th></th>
						<th></th>
						<th></th>
						<th></th>
						<th></th>
					</tr>
				</thead>
				<tr>
					<td></td>
					<td><%= @books.title %></td>
					<td><%= @books.body %></td>
					<td><%= link_to 'edit', edit_book_path(@books.id), class: "edit_book_#{@book.id}" %></td>
					<td><%= link_to 'destroy', book_path(@books), method: :delete, class: "destroy_book_#{@book.id}" %></td>
				</tr>
			</table>

		</div>

	</div>
</div>