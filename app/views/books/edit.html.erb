<%= render 'users/sidebar', user: @user, book: @book %>

<h1>Editing Book</h1>
	<%= form_for(book) do |f| %>
		<h4>Title</h4>
		<%= f.text_field :title %>
		<h4>Opinion</h4>
		<%= f.text_area :body %>
		<%= f.submit 'Update Book', class: 'btn-group-justified' %>
	<% end %>s